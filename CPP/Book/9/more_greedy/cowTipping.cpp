#include <bits/stdc++.h>

using namespace std;

int n;
bool cows[10][10];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("cowtip.in", "r", stdin);
    freopen("cowtip.out", "w", stdout);


    for (int i = 0; i < n; i++) {
        for (int k = 0; k < n; k++) {
            cin >> cows[i][k];
        }
    }

    
}